import Foundation
import AgentStreemrSwift

/// Registers protocol-log tool handlers on the given coordinator.
/// The protocol log is read-only; no local tools are needed for it.
func registerProtocolLogTools(coordinator: LocalToolCoordinator) async {
    // No local tools required.
}
